package com.ourcraft.ecs.components;

import com.simsilica.es.EntityComponent;

// Stub for M1 — M2 adds blockType, durability, maxDurability
public record BlockComponent() implements EntityComponent {}
